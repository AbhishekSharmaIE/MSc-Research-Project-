apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-b
  namespace: tenant-b
  labels:
    app: web-b
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-b
  template:
    metadata:
      labels:
        app: web-b
    spec:
      containers:
      - name: nginx
        image: nginx:stable
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d
      volumes:
      - name: nginx-config
        configMap:
          name: nginx-config-b
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config-b
  namespace: tenant-b
data:
  default.conf: |
    server {
        listen 80;
        server_name _;
        
        location / {
            return 200 "Tenant B - Default Path\n";
            add_header Content-Type text/plain;
        }
        
        location /status {
            return 200 "Tenant B - Status OK\n";
            add_header Content-Type text/plain;
        }
        
        location /api {
            return 200 "Tenant B - API Endpoint\n";
            add_header Content-Type text/plain;
        }
    }

